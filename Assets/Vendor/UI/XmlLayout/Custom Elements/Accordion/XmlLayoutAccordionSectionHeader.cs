using UnityEngine;

namespace UI.Xml
{
    [ExecuteInEditMode]
    public class XmlLayoutAccordionSectionHeader : XmlLayoutElementBase
    {
        /*private XmlLayoutAccordion _accordion = null;
        public XmlLayoutAccordion accordion
        {
            get
            {
                if (_accordion == null) _accordion = GetComponentInParent<XmlLayoutAccordion>();

                return _accordion;
            }
        }

        private XmlLayoutAccordionSection _section = null;
        internal XmlLayoutAccordionSection section
        {
            get
            {
                if (_section == null) _section = GetComponentInParent<XmlLayoutAccordionSection>();

                return _section;
            }
        }

        private bool isCurrentlyBeingDragged
        {
            get
            {
                return accordion.sectionBeingDragged == this.section;
            }
        }*/
    }
}
